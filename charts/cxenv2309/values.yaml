edc-provider:
  participant:
    id: provider
  vault:
    hashicorp:
      token: "XXXXXX"
  controlplane:
    endpoints:
      management:
        authKey: "XXXXXX"
  backendService:
    httpProxyTokenReceiverUrl: "XXXXXX"


edc-consumer:
  participant:
    id: consumer
  vault:
    hashicorp:
      token: "XXXXXX"
  controlplane:
    endpoints:
      management:
        authKey: "XXXXXX"
  backendService:
    httpProxyTokenReceiverUrl: "XXXXXX"


miw:
  serviceAccount:
    # --  Enable creation of ServiceAccount
    create: false
    # -- Annotations to add to the ServiceAccount
    annotations: {}
    # -- The name of the ServiceAccount to use.
    name: "xxxxxx.com"
